# Selected Story

- Story Key: `story-2.1`
- Epic Key: `epic-2`
- Title: `Chat-first inbox surface and composer shell`

# Quality Plan

| Criterion ID | Intended Code/Test/Artifact Evidence |
| --- | --- |
| `story-2.1:GOAL-1` | Reshape `apps/desktop/src/renderer/routes/ChatRoute.tsx` into a chat-first inbox surface where the product purpose and first Korean request action are visible together. |
| `story-2.1:SCOPE-1` | `apps/desktop/src/renderer/routes/ChatRoute.tsx`, `apps/desktop/src/renderer/routes/chat-surface.ts`, and `apps/desktop/src/renderer/styles.css` now own the welcome copy, inbox starter cards, and Korean composer shell. |
| `story-2.1:SCOPE-2` | `apps/desktop/src/renderer/routes/ChatRoute.tsx` keeps model selection and optimization mode controls inside the same composer dock. |
| `story-2.1:SCOPE-3` | `apps/desktop/src/renderer/routes/chat-surface.ts` plus `ChatRoute.tsx` define the empty-state and CTA copy that tells the user what to do first. |
| `story-2.1:AC-1` | `chatSurfaceCopy.headline`, `chatSurfaceCopy.intro`, and the `conversation-stage` header in `ChatRoute.tsx` expose the Korean welcome message and first-action guidance. |
| `story-2.1:AC-2` | `chatStarterCards` in `apps/desktop/src/renderer/routes/chat-surface.ts` ship the required examples for business-plan drafting, long PDF summary, and copy polishing. |
| `story-2.1:AC-3` | `ChatRoute.tsx` renders the multiline Korean textarea plus `modelOptions` and `optimizationModeOptions` in the same `composer-dock` surface. |
| `story-2.1:AC-4` | `chatSurfaceCopy.recentInboxEmptyBody` and the `empty-state-card` in `ChatRoute.tsx` explain how to begin when the inbox is empty. |
| `story-2.1:AC-5` | `styles.css` adds the `chat-inbox-shell` desktop split and removes the chat screen’s hero-card reliance in favor of starter rail + conversation stage + composer dock. |
| `story-2.1:AC-6` | `starter-card` styling and `createStarterDraftSelection(...)` make starter actions behave like inbox starters that prefill the composer. |
| `story-2.1:AC-7` | `styles.css` keeps the white/blue/mint palette while using softer gradients and conversation surfaces to avoid a cold dashboard tone. |
| `story-2.1:AC-8` | `chatSurfaceCopy.primaryCta` is `한국어 요청 시작하기`, and `ChatRoute.tsx` uses that label for the main action. |
| `story-2.1:VAL-1` | `apps/desktop/tests/chat-surface.test.ts` asserts the first-screen headline, product explanation, empty-state guidance, and primary CTA copy. |
| `story-2.1:VAL-2` | `apps/desktop/tests/chat-surface.test.ts` verifies the required starter examples and that starter selection prefills the composer draft via `createStarterDraftSelection(...)`. |
| `story-2.1:VAL-3` | `apps/desktop/tests/chat-surface.test.ts` asserts the `chat-inbox-shell` layout hooks and desktop split CSS; live browser verification is recorded as `GUIDED_VERIFICATION_REQUIRED` because the local Vite binary is unavailable in this workspace. |

# In Scope for This Run

- `story-2.1` chat-first inbox surface
- Recommended inbox starter actions
- Korean composer shell with model and optimization selectors
- Story-local validation script and evidence

# Deferred Work

- Run progress, response rendering, dashboard/history detail, and workbench handoff remain outside `story-2.1`
- Dashboard/history, workbench, project, and settings behavior beyond first-screen shell changes
- Personalized starter suggestions
- Live browser screenshot verification is deferred as `GUIDED_VERIFICATION_REQUIRED` because the workspace cannot start Vite locally

# Validation Performed

- `npm test`
  - Result: PASS
- `npm run validate:story-2.1`
  - Result: PASS
- `npm --workspace @talkin-ai/desktop run typecheck`
  - Result: FAIL
  - Reason: pre-existing `TS2688 Cannot find type definition file for 'vite/client'`; unchanged by this story
- `npm --workspace @talkin-ai/desktop run dev:renderer`
  - Result: FAIL
  - Reason: `sh: vite: command not found`, so automated browser verification could not be executed in this workspace

# Criterion Assessment

- `story-2.1:GOAL-1` PASS: the first screen now reads as an inbox workspace with starters on the left and a ready-to-use Korean composer on the right.
- `story-2.1:SCOPE-1` PASS: welcome copy, starter actions, and the Korean composer shell were all delivered on the `Chat` route.
- `story-2.1:SCOPE-2` PASS: model and optimization selectors remain inside the same working composer surface.
- `story-2.1:SCOPE-3` PASS: the empty inbox and the main CTA both explain the first safe action.
- `story-2.1:AC-1` PASS: the route header states the Korean-first purpose and the local optimization behavior.
- `story-2.1:AC-2` PASS: starter content explicitly covers business-plan drafting, long PDF summary, and copy polishing.
- `story-2.1:AC-3` PASS: the route keeps the textarea, model controls, and mode controls together in the same composer dock.
- `story-2.1:AC-4` PASS: empty-state copy tells the user to click a starter or type a first Korean request.
- `story-2.1:AC-5` PASS: the screen silhouette is now left navigation + inbox starter rail + conversation/composer stage rather than a centered landing card.
- `story-2.1:AC-6` PASS: starter cards act as action rows that prefill the composer instead of acting like neutral dashboard cards.
- `story-2.1:AC-7` PASS: the screen stays white-based with blue and mint accents, but the surfaces are softened to keep the chat workspace warmer.
- `story-2.1:AC-8` PASS: the primary CTA is Korean request-start language, not a generic submit label.
- `story-2.1:VAL-1` PASS: automated tests cover product-purpose and first-action copy directly from the shared screen content model.
- `story-2.1:VAL-2` PASS: automated tests prove the starter examples and the composer-prefill selection path.
- `story-2.1:VAL-3` PASS with `GUIDED_VERIFICATION_REQUIRED`: source and CSS assertions cover the desktop split shell; live in-browser confirmation is still blocked by missing local Vite tooling.

# Design Compliance Notes

- Status: `pass`
- Relevant contract coverage:
  - The selected slice preserves the left global navigation and makes the `Chat` route a real workspace first screen.
  - The screen shows the required welcome message, starter examples, Korean composer, model selector, and optimization mode selector.
  - Empty state and CTA copy both tell the user exactly how to begin the first request.
- Remaining weak spots:
  - Live browser confirmation is pending because the workspace cannot launch the Vite renderer locally.

# Design Differentiation Notes

- Status: `pass`
- Intended archetype: `chat-first inbox`
- Visible choices made: converted starter suggestions into an inbox starter rail, moved the product explanation into a conversation-stage header, and kept the composer dock ready at the bottom of the main work area.
- Layout silhouette difference from default/control: the first screen now reads as left navigation + starter rail + message lane/composer stage instead of a centered hero card or generic dashboard tiles.
- Default convergence risks avoided: removed reliance on the old hero-stat card, kept the workspace warm with soft gradients, and made cards behave like action rows rather than marketing tiles.
- Remaining weak spots: browser-based visual QA is still pending, so the delivered differentiation is proven by source and CSS assertions rather than a captured live viewport.

# Outcome Quality Notes

- Purpose clarity: the first screen now states that users can ask in Korean while the product handles local optimization under the hood.
- Primary action: the main action is specific and immediate, `한국어 요청 시작하기`, with starter rows that prefill the composer.
- State/copy handling: the empty state explains the first step, the preview mode explains why browser-only rendering cannot submit, and submit messages keep the user informed when the desktop bridge is available.
- Visible quality choices: starter rows, recent inbox rail, draft preview bubble, and the composer dock make the chat surface feel like a working inbox instead of a marketing panel.
- Remaining weak spots: visual verification in a running browser session is still blocked locally, and response rendering/progress states remain intentionally deferred to later stories.

# Structured Assessment

```json
{
  "success_criteria_status": "pass_for_story_slice_with_guided_visual_verification_required",
  "story_completion_status": "completed",
  "mvp_acceptance_status": "pass_for_story_slice",
  "release_evidence_status": "pass_with_guided_visual_verification_required",
  "package_readiness_status": "warning_preexisting_vite_client_type_gap_and_missing_local_vite_bin",
  "remaining_required_story_keys": [],
  "next_story_candidates": [
    "story-2.2"
  ],
  "risks": [
    "Live browser verification could not run because the workspace cannot start the Vite renderer (`vite: command not found`).",
    "Renderer typecheck still fails on the pre-existing `vite/client` type-definition gap, so package readiness is not fully green.",
    "Prompt execution, response rendering, and workbench handoff remain deferred by story scope even though the first-screen shell is now in place."
  ]
}
```

# Selected Story

- Story Key: `story-2.2`
- Epic Key: `epic-2`
- Title: `Durable Korean prompt submission and chat history preservation`

# Quality Plan

| Criterion ID | Intended Code/Test/Artifact Evidence |
| --- | --- |
| `story-2.2:GOAL-1` | Route the chat composer through the persistent `submitPrompt` path and render saved Korean messages from the durable chat feed in `apps/desktop/src/renderer/routes/ChatRoute.tsx`. |
| `story-2.2:SCOPE-1` | `apps/desktop/src/renderer/routes/ChatRoute.tsx` and `apps/desktop/src/renderer/routes/chat-surface.ts` own the composer submit flow, safe submit states, and feed transition behavior. |
| `story-2.2:SCOPE-2` | `apps/desktop/src/main/chat/service.ts` remains the durable write path; `apps/desktop/tests/chat-history.test.ts` verifies persisted `task`, `conversation`, `message`, `run_record`, and `run_stage` rows through `createDesktopIpcService(...)`. |
| `story-2.2:SCOPE-3` | `ChatRoute.tsx` now renders persisted `messages.contentKo` in the conversation lane, while `styles.css` preserves multiline Korean content with `white-space: pre-wrap`. |
| `story-2.2:AC-1` | `apps/desktop/tests/chat-history.test.ts` proves `submitPrompt` writes durable rows and `getChatFeed` immediately reflects the saved task, conversation, message, and queued run. |
| `story-2.2:AC-2` | `apps/desktop/tests/chat-history.test.ts` and `apps/desktop/tests/chat-surface.test.ts` assert the exact raw Korean draft is sent and stored without trimming away the original content. |
| `story-2.2:AC-3` | `apps/desktop/tests/chat-history.test.ts` recreates the IPC service against the same SQLite file and verifies the long Korean prompt reappears unchanged after restart. |
| `story-2.2:AC-4` | `apps/desktop/tests/chat-surface.test.ts` injects a failed local save into the renderer submit helper and verifies the draft stays intact with safe Korean error copy. |
| `story-2.2:AC-5` | `apps/desktop/src/renderer/routes/ChatRoute.tsx` switches the stage from the empty guide card to a real conversation feed once saved or pending-saved messages exist; `apps/desktop/tests/chat-surface.test.ts` locks that source behavior. |
| `story-2.2:AC-6` | The renderer shows a saving bubble before the input clears and an optimistic saved bubble after durable save success; helper tests verify the feed can stay populated while the query catches up. |
| `story-2.2:VAL-1` | `npm test` / `npm run validate:story-2.2` cover durable submit plus DB and feed assertions in `apps/desktop/tests/chat-history.test.ts`. |
| `story-2.2:VAL-2` | `apps/desktop/tests/chat-history.test.ts` provides recreate-and-readback evidence against the same SQLite file. |
| `story-2.2:VAL-3` | `apps/desktop/tests/chat-surface.test.ts` injects a failed submit promise and verifies draft preservation and safe error state. |

# In Scope for This Run

- Durable Korean prompt submission through the desktop chat composer
- Persistent task/conversation/message/run/stage creation before execution
- Chat-stage rendering of saved Korean source history, including multiline content
- Safe renderer submit states for saving and local-write failure

# Deferred Work

- Local optimization, cloud inference, and Korean response restoration remain out of scope for `story-2.2`
- Per-request savings accuracy and usage-detail UI remain placeholder behavior outside this slice
- Live desktop-width viewport verification is still `GUIDED_VERIFICATION_REQUIRED` because the workspace cannot start the Vite renderer locally (`vite: command not found`)

# Validation Performed

- `npm test`
  - Result: PASS
- `npm run validate:story-2.2`
  - Result: PASS
- `npm --workspace @talkin-ai/desktop run typecheck`
  - Result: FAIL
  - Reason: pre-existing `TS2688 Cannot find type definition file for 'vite/client'`
- `npm --workspace @talkin-ai/desktop run dev:renderer`
  - Result: FAIL
  - Reason: pre-existing local tooling gap, `vite: command not found`

# Criterion Assessment

- `story-2.2:GOAL-1` PASS: Korean prompt submission now produces durable saved state before the UI transitions into the chat feed.
- `story-2.2:SCOPE-1` PASS: the composer submit command is wired through renderer helpers and the desktop IPC submit command.
- `story-2.2:SCOPE-2` PASS: durable task, conversation, message, run, and queued stage creation is proven through SQLite-backed IPC tests.
- `story-2.2:SCOPE-3` PASS: the chat stage renders saved Korean message bodies directly and preserves multiline formatting.
- `story-2.2:AC-1` PASS: after submit, the feed reflects the saved conversation and queued run backed by persistent storage.
- `story-2.2:AC-2` PASS: the raw Korean draft is submitted and stored exactly as entered instead of sending only a trimmed variant.
- `story-2.2:AC-3` PASS: a recreated desktop IPC service restores the same long Korean history from the same database file.
- `story-2.2:AC-4` PASS: failed local save keeps the draft and returns safe Korean guidance instead of dropping user input.
- `story-2.2:AC-5` PASS with `GUIDED_VERIFICATION_REQUIRED`: the route source and tests now switch from landing copy to a conversation feed when saved content exists, but live viewport confirmation is blocked by missing local Vite tooling.
- `story-2.2:AC-6` PASS with `GUIDED_VERIFICATION_REQUIRED`: saving and saved states are represented in the conversation lane before/while the query refreshes, but live interaction verification is still blocked locally.
- `story-2.2:VAL-1` PASS: automated tests confirm durable submit and exact Korean round-trip in DB and feed projections.
- `story-2.2:VAL-2` PASS: automated recreate-and-readback coverage confirms restart restoration.
- `story-2.2:VAL-3` PASS: failure-injection coverage confirms draft preservation and safe error messaging.

# Design Compliance Notes

- Status: `weak`
- Relevant contract coverage:
  - The chat-first inbox shell from `story-2.1` is preserved while the stage now transitions into a real conversation feed after the first saved request.
  - The user still starts in Korean, and the screen continues to present the welcome message, starter rail, model selector, and optimization mode selector.
  - Saving feedback now appears in user-facing language before the composer clears, which aligns with the trust requirement for the first submit flow.
- Remaining weak spots:
  - `story-2.2:AC-5` and `story-2.2:AC-6` still need live desktop-width confirmation because the local Vite renderer cannot be started in this workspace.

# Design Differentiation Notes

- Status: `weak`
- Intended archetype: `chat-first inbox`
- Visible choices made: preserved the starter rail and inbox silhouette, replaced the static preview lane with saved-message bubbles, and added saving/saved states directly inside the conversation stage.
- Layout silhouette difference from default/control: the first submit now turns the right pane into a message feed inside the existing inbox workspace instead of keeping the user on a hero-like landing state.
- Default convergence risks avoided: the screen still reads as an inbox/workspace rather than a centered form or generic dashboard, and saved-message bubbles keep the product warmer than a plain data table.
- Remaining weak spots: this differentiation is proven by source and automated assertions only; live viewport evidence is still blocked by missing local Vite tooling.

# Outcome Quality Notes

- Purpose clarity: the chat screen still explains the Korean-first value proposition while showing that the first submit becomes durable chat history.
- Primary action: the main action remains a Korean-specific request start, and the first saved request now immediately becomes visible as a feed item or saving bubble.
- State/copy handling: empty state, saving state, saved state, and local-write failure state now have explicit Korean copy; the safe failure state preserves the user draft.
- Visible quality choices: multiline Korean content is rendered with preserved line breaks, and the conversation stage now shows durable history rather than only a static preview bubble.
- Remaining weak spots: live UI verification is blocked by missing Vite tooling, and the pre-existing `vite/client` type-definition gap still prevents a clean typecheck baseline.

# Structured Assessment

```json
{
  "success_criteria_status": "pass_for_story_slice_with_guided_visual_verification_required",
  "story_completion_status": "completed_with_guided_visual_verification_required",
  "mvp_acceptance_status": "pass_for_story_slice",
  "release_evidence_status": "pass_with_guided_visual_verification_required",
  "package_readiness_status": "warning_preexisting_vite_client_type_gap_and_missing_local_vite_bin",
  "remaining_required_story_keys": [],
  "next_story_candidates": [
    "story-3.1"
  ],
  "risks": [
    "Desktop-width live verification for the first-submit feed transition is still blocked because `npm --workspace @talkin-ai/desktop run dev:renderer` fails with `vite: command not found`.",
    "Package typecheck still fails on the pre-existing `vite/client` type-definition gap, so the workspace is not fully green outside test coverage.",
    "Only the durable submit/history slice was implemented here; optimization, cloud execution, and restored Korean responses remain deferred by story scope."
  ]
}
```

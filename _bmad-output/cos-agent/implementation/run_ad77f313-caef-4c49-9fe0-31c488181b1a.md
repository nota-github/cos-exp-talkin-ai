# Selected Story

- Story Key: `story-1.5`
- Epic Key: `epic-1`
- Title: `Event broadcast and renderer query invalidation baseline`

# Quality Plan

| Criterion ID | Implemented Evidence |
| --- | --- |
| `story-1.5:GOAL-1` | `apps/desktop/src/main/ipc/register-ipc.ts` now stages command mutations on a cloned canonical state and emits invalidation only after commit, while `apps/desktop/src/renderer/lib/ipc/query-client.ts` re-reads through IPC queries instead of relying on screen-local mutation. |
| `story-1.5:SCOPE-1` | `commitCommandMutation(...)` in `apps/desktop/src/main/ipc/register-ipc.ts` centralizes post-commit invalidation dispatch for command writes. |
| `story-1.5:SCOPE-2` | `apps/desktop/src/renderer/lib/ipc/query-client.ts` and `apps/desktop/src/renderer/lib/ipc/query-hooks.ts` add a renderer query cache plus invalidation subscription, including queued post-flight refetch when invalidation arrives during an in-flight IPC read; `apps/desktop/src/renderer/routes/ChatRoute.tsx` consumes live query data. |
| `story-1.5:SCOPE-3` | `commitCommandMutation(...)` now resolves optional commit work only after the injected commit hook passes, so `updateSettings` persistence no longer escapes the rollback boundary. |
| `story-1.5:AC-1` | Successful `submitPrompt` commits emit `task`, `conversation`, `run`, `chatFeed/historyEntry`, and `usageDashboard(month/all_time)` invalidation targets from the shared mutation boundary. |
| `story-1.5:AC-2` | `apps/desktop/tests/ipc-contracts.test.ts` verifies forced commit rollbacks emit no invalidation event and leave both `getChatFeed` and `getSettings` unchanged. |
| `story-1.5:AC-3` | `DesktopQueryCache` refetches registered queries from IPC on invalidation, and now queues one more source-of-truth read if the matching query is already loading; `ChatRoute` no longer appends inbox rows locally after submit. |
| `story-1.5:AC-4` | `ChatRoute` submits Korean input through IPC and reads the updated inbox from `getChatFeed`; the smoke test confirms the chat feed becomes four items only after refetch. |
| `story-1.5:VAL-1` | `story-1.5:VAL-1 and story-1.5:VAL-3 renderer query cache refetches chat feed through main/preload invalidation flow`, `story-1.5:AC-1 and story-1.5:VAL-1 submitPrompt invalidates usage dashboard queries`, and `story-1.5:VAL-1 and story-1.5:VAL-3 queues a second refetch when invalidation arrives during in-flight queries` in `apps/desktop/tests/ipc-contracts.test.ts`. |
| `story-1.5:VAL-2` | `story-1.5:VAL-2 failed commit does not broadcast invalidation or mutate renderer query state` plus `story-1.5:SCOPE-3 and story-1.5:VAL-2 failed updateSettings commit leaves settings query unchanged` in `apps/desktop/tests/ipc-contracts.test.ts`. |
| `story-1.5:VAL-3` | Same main/preload/renderer test path as `story-1.5:VAL-1`, using `registerDesktopIpcHandlers`, `createTalkinAIDesktopApi`, `createRendererDesktopClient`, and `DesktopQueryCache`. |

# In Scope for This Run

- Success-only invalidation dispatch from the main-process command layer
- Renderer-side query invalidation and refetch baseline
- Chat feed smoke wiring for one concrete projection path
- Story-local validation script wiring for `validate:story-1.5`

# Deferred Work

- Replace the in-memory IPC demo state with SQLite-backed projection queries in later stories
- Detailed live query wiring for Workbench, Projects, Usage, and Settings screens
- Offline or multi-device sync behavior
- Pre-existing renderer `typecheck` gap caused by missing `vite/client` type resolution

# Validation Performed

- `npm test`
  - Result: PASS
- `npm run validate:story-1.5`
  - Result: PASS
- `npm --workspace @talkin-ai/desktop run typecheck`
  - Result: FAIL
  - Reason: pre-existing `TS2688 Cannot find type definition file for 'vite/client'`; unchanged by this story

# Criterion Assessment

- `story-1.5:GOAL-1` PASS: command writes now commit into one canonical IPC state before the broadcast is emitted, and renderer cache refresh uses IPC query re-read as the normal path.
- `story-1.5:SCOPE-1` PASS: command handlers were moved behind a shared `commitCommandMutation(...)` boundary.
- `story-1.5:SCOPE-2` PASS: renderer invalidation wiring lives in a reusable query cache plus hook layer, with queued post-flight refetch so matching invalidations are not dropped during loading.
- `story-1.5:SCOPE-3` PASS: `updateSettings` persistence now runs after the injected commit hook, so failed commits leave both stored settings and renderer state unchanged.
- `story-1.5:AC-1` PASS: successful `submitPrompt` emits invalidation that reaches renderer subscribers for both chat feed and usage dashboard queries.
- `story-1.5:AC-2` PASS: forced rollback leaves the canonical query results unchanged for both `submitPrompt` and `updateSettings`.
- `story-1.5:AC-3` PASS: the refetch proof is the second IPC query invocation counted by the integration tests, including the delayed-response case where committed data wins after a stale first read settles.
- `story-1.5:AC-4` PASS: the first screen can submit a Korean prompt and then shows the refreshed inbox projection.
- `story-1.5:VAL-1` PASS: automated invalidation/refetch tests now cover both normal and in-flight-delayed refresh paths for chat feed and usage dashboard queries.
- `story-1.5:VAL-2` PASS: automated rollback/no-false-success tests now cover both `submitPrompt` and `updateSettings`.
- `story-1.5:VAL-3` PASS: automated main/preload/renderer integration flow exercised, including queued refetch after delayed stale IPC reads.

# Design Compliance Notes

- Status: `pass`
- Relevant contract coverage:
  - The first screen remains a chat-first workspace with the required welcome copy.
  - The screen still shows recommended Korean prompts, a Korean prompt input, model selection, and optimization-mode controls.
  - The user-facing update stayed scoped to the chat screen touched by this story; unrelated screens were not redesigned.
- Remaining weak spots:
  - Browser preview still falls back to non-desktop placeholder behavior when preload is unavailable.

# Design Differentiation Notes

- Status: `pass`
- Intended archetype: `chat-first inbox`
- Visible choices made: live inbox previews stay in the left rail, composer remains the primary pane, and successful submit messaging explains that the inbox is refreshed from the shared source of truth.
- Layout silhouette difference from default/control: still a split inbox/composer desktop layout rather than a centered form or neutral dashboard card.
- Default convergence risks avoided: kept the warm chat-inbox framing and did not replace it with a cold CRUD dashboard while wiring live data.
- Remaining weak spots: only the chat feed is live in this slice; the rest of the workspace is still placeholder data.

# Outcome Quality Notes

- Purpose clarity: the first screen still explains Korean-first input and local optimization, while now showing a live inbox that proves state changes.
- Primary action: the user can submit the main item from the Korean composer and see clear progress/success/error messaging.
- State/copy handling: loading shows inbox sync, empty state explains what to do first, error copy explains the failed refresh/write in user-facing Korean, and success copy confirms that the inbox will refresh from a new query.
- Visible quality choices: refetch-driven updates are explicit in code and tests, including the case where a success event lands while a stale read is already in flight.
- Remaining weak spots: the renderer typecheck issue is still external to this story, and most route components beyond chat still render placeholder data even though the baseline invalidation coverage now includes usage dashboards.

# Structured Assessment

```json
{
  "success_criteria_status": "pass_for_story_slice",
  "story_completion_status": "completed",
  "mvp_acceptance_status": "pass_for_story_slice",
  "release_evidence_status": "pass_with_preexisting_typecheck_warning",
  "package_readiness_status": "warning_preexisting_vite_client_type_gap",
  "remaining_required_story_keys": [],
  "next_story_candidates": [
    "story-2.1",
    "story-3.1"
  ],
  "risks": [
    "Most route components beyond chat still render placeholder data even though the invalidation baseline now covers chat and usage-dashboard queries.",
    "The workspace still has a pre-existing renderer typecheck blocker for vite/client type resolution, so package readiness is not fully green even though validation passed."
  ]
}
```

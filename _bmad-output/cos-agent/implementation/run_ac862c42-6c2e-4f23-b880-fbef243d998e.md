## Selected Story

- Story Key: `story-6.4`
- Epic Key: `epic-6`
- Title: `Cross-screen projection sync hardening`

## Quality Plan

- `story-6.4:GOAL-1`
  - Code: surface cached-data sync failures on already-loaded `Workbench`, project hub, kanban, and project detail screens without dropping the last good projection.
  - Tests: prove the same canonical task data remains visible while the UI switches into an explicit stale-sync warning state.
- `story-6.4:SCOPE-1`
  - Code: tighten projection surface-state helpers so query status and cached data are interpreted consistently across screens.
  - Tests: add surface-state assertions for `error + cached data` and `error + no data`.
- `story-6.4:SCOPE-2`
  - Code: keep the existing invalidation/refetch path, but add visible retry affordances anywhere a refetch can fail after cached data already exists.
  - Tests: drive invalidated refetch failures through the renderer query cache and verify the affected surfaces expose retry-safe warning states.
- `story-6.4:SCOPE-3`
  - Tests: extend `story-6.4` regression coverage with cached-projection failure scenarios for workbench, project hub, kanban, and project detail.
- `story-6.4:AC-1`
  - Tests: preserve the existing cross-screen convergence scenario so one task still resolves to the same id/state across chat, workbench, kanban, and project detail.
- `story-6.4:AC-2`
  - Tests: preserve the existing mutation-driven convergence assertions after status changes, project linkage changes, and follow-up prompt submission.
- `story-6.4:AC-3`
  - Code: remove screen-local stale/error branching that can silently hide refetch failures behind cached content with no sync signal.
  - Tests: assert the same cached projection state machine is shared across the affected route helpers.
- `story-6.4:AC-4`
  - Code: show a visible stale-sync warning plus retry action when refetch fails after a screen has already loaded data.
  - Tests: assert the warning state activates instead of the UI presenting only stale content.
- `story-6.4:VAL-1`
  - Tests: keep the existing integrated task flow across chat, workbench, kanban, and project detail.
- `story-6.4:VAL-2`
  - Tests: keep invalidation event assertions from the integrated query-cache regression.
- `story-6.4:VAL-3`
  - Tests: add a renderer-query-cache stale-projection scenario that starts from success, injects failing invalidated refetches, and verifies warning/retry UI state on cached data.

## In Scope for This Run

- Cached-data sync error visibility on already-loaded `Workbench`, project hub, kanban, and project detail screens
- Regression coverage for invalidated refetch failures that preserve last good data
- Copy polish for warning and retry states within the existing desktop workspace surfaces

## Deferred Work

- New product scope outside `story-6.4`
- Tooling fixes unrelated to the current test path

## Implementation Summary

- `apps/desktop/src/renderer/lib/ipc/error-copy.ts`
  - Added a small renderer-side safe-copy mapper so bridge/context failures, `Unknown task/project/run`, and timeout-style errors resolve to Korean-first primary guidance.
  - Preserved raw error strings only as optional diagnostics behind a non-primary disclosure for `story-6.4:AC-4`.
- `apps/desktop/src/renderer/routes/workbench-surface.ts`
  - Added a distinct cached-data sync-warning state for `story-6.4:AC-4` so `Workbench` no longer treats `error + existing layout` as normal success with no warning.
  - Rewrote touched rail/stage copy to Korean-first product language so the surface no longer mixes visible English scaffold labels or internal jargon.
- `apps/desktop/src/renderer/routes/WorkbenchRoute.tsx`
  - Added a shared `refreshWorkbenchLayout()` retry path and rendered stale-sync warning cards in both the recent-task rail and the stage header while keeping the active panels visible.
  - Mapped warning/error primaries through the new safe-copy helper and moved raw details into an opt-in diagnostic disclosure instead of a primary badge.
  - Replaced the remaining visible `Conversation` panel kicker and raw conversation-id badge with Korean product copy so the touched workbench surface no longer leaks English scaffold text or opaque internal identifiers.
- `apps/desktop/src/renderer/routes/projects-surface.ts`
  - Added `showSyncWarningState` handling to project hub and kanban surface-state helpers for `story-6.4:SCOPE-1`.
  - Added `getProjectDetailSurfaceState()` so project-detail loading, hard error, and cached-data warning logic use the same state-machine shape as the other projections.
- `apps/desktop/src/renderer/routes/ProjectsRoute.tsx`
  - Added retry-safe warning banners for project hub, project detail, and kanban when invalidated refetches fail after data has already loaded.
  - Kept the last good project/task/file context visible instead of dropping the workspace back to an empty/error-only shell.
  - Normalized the touched project-hub/detail copy from mixed `task` wording to Korean `작업`, and routed sync-error primaries through the same safe-copy helper with optional diagnostics.
  - Stopped rendering raw `task.sourceScreen` in the project command surface and now map the label through `getProjectTaskSourceLabel(...)` like the rest of project detail.
- `apps/desktop/src/renderer/styles.css`
  - Aligned warning CTA buttons to the start edge so retry actions read like workspace controls rather than stretched form buttons.
  - Added compact diagnostic disclosure styling so raw details stay secondary to the primary Korean recovery message.
- `apps/desktop/tests/projection-sync.test.ts`
  - Added a new `story-6.4:VAL-3` regression that starts from successful queries, forces projection refetch failures after invalidation, asserts cached warning states across `Workbench`, project hub, kanban, and project detail, then verifies manual retry converges to fresh source-of-truth data.
  - Tightened the `story-6.4:VAL-1` / `story-6.4:VAL-2` cross-screen convergence wait condition so the regression now waits for shared `lastActivityAt` alignment across chat, workbench, kanban, project list, and project detail before asserting final canonical state.
- `apps/desktop/tests/workbench-route.test.ts`
  - Added direct state/copy coverage for cached workbench warning behavior, Korean-first label cleanup, safe diagnostic disclosure handling, and the composer badge copy.
  - Added explicit regression assertions that fail if the `Conversation` panel kicker or a raw conversation-id badge reappears on the touched workbench surface.
- `apps/desktop/tests/projects-route.test.ts`
  - Added direct surface-state and copy coverage for cached project hub, board, and project-detail warning behavior, explicitly satisfying `story-6.4:SCOPE-3`.
  - Added a route-source assertion that the project command surface uses `getProjectTaskSourceLabel(task.sourceScreen)` and no longer prints raw `sourceScreen` values.

## Validation Performed

- `npm test`
  - Result: `PASS`
  - Evidence:
    - `116` tests passed, `0` failed.
    - `apps/desktop/tests/projection-sync.test.ts` covers `story-6.4:VAL-1`, `story-6.4:VAL-2`, and the invalidated-refetch failure path for `story-6.4:VAL-3`.
    - `apps/desktop/tests/workbench-route.test.ts` now fails if the touched workbench surface reintroduces the English `Conversation` kicker or a raw conversation-id badge.
    - `apps/desktop/tests/projects-route.test.ts` now fails if the touched project command surface renders raw `task.sourceScreen` instead of `getProjectTaskSourceLabel(...)`.
  - Notes:
    - The suite still emits existing `MODULE_TYPELESS_PACKAGE_JSON` warnings from Node test execution, but the command completed successfully and no functional assertions failed.

## Design Compliance Notes

- Status: `pass`
- Notes:
  - The touched flows remain inside the requested desktop workspace model: warnings appear inside the existing `Workbench` and project/kanban control surfaces instead of via a detached generic toast.
  - Korean-first copy is now explicit across the touched workbench and project surfaces, including the previously mixed `task` wording and visible English scaffold labels.
  - Retry actions and primary failure copy are phrased as workspace-safe resync steps rather than developer-facing error text, while raw diagnostics stay secondary.
  - No must-have navigation or workspace structure was removed or replaced.

## Design Differentiation Notes

- Status: `weak`
- Intended archetype: `chat-first inbox`
- Visible choices made: stale-sync warnings live inside the rail/stage/hub surfaces that own the data, so the product still reads as one workspace rather than a generic alert system layered on top.
- Layout silhouette difference from default/control: unchanged overall; this run intentionally preserved the existing workbench and project silhouettes while tightening their state feedback.
- Default convergence risks avoided: avoided generic “data may be stale” admin copy and used task/project/workbench-specific Korean guidance with explicit retry actions.
- Remaining weak spots: this run did not add fresh live Browser/Electron screenshot evidence, so differentiation is source-and-test backed rather than visually re-verified.

## Outcome Quality Notes

- Purpose clarity: improved for stale-sync moments. Users now see when they are looking at the last confirmed workspace state rather than assuming the visible task/project data is freshly synced.
- Primary action: preserved. The main actions remain continuing the same task, moving status, or editing project context; retrying sync is a secondary safe action attached only when needed, and the workbench composer badge now reinforces task continuity instead of exposing an opaque conversation identifier.
- State/copy handling: `story-6.4:AC-4` is now explicit across `Workbench`, project hub, kanban, and project detail. Cached data stays visible, retry guidance and failure context are also visible, and the touched workbench composer no longer exposes raw internal identifier copy in its visible badge.
- Visible quality choices: warnings are inline, Korean, and scoped to the owning surface. Retry CTAs sit inside the same panel where the stale state is being shown, and technical details sit behind an opt-in disclosure instead of a badge while the composer badge stays product-facing.
- Remaining weak spots: this run does not include live rendered proof, and it does not address unrelated package/toolchain warnings outside the test path.

## ProductQualityAssessment

```json
{
  "status": "pass",
  "focus_dimensions": [
    "purpose_clarity",
    "copy_quality",
    "completion_value"
  ],
  "notes": [
    "Cached-data refetch failures now explain that the user is seeing the last synced workspace state.",
    "Primary error copy is now Korean-first and product-safe, with raw diagnostics moved behind an opt-in detail disclosure.",
    "Touched workbench and project surfaces no longer leak the visible `Conversation` kicker, raw conversation identifier badge, or raw `sourceScreen` values on the repaired story-6.4 surfaces.",
    "Completion value improved because users can keep working from the last good task state while getting a clear resync path."
  ]
}
```

## Repair Result Summary

- `reproduction_status`: `confirmed`
  - `apps/desktop/src/renderer/routes/WorkbenchRoute.tsx` still rendered the visible raw badge ``대화 ID ${panel.conversation.conversationId}`` in the workbench composer header.
- `repair_result_status`: `fixed`
  - The workbench composer badge now uses Korean product copy that tells the user the same task continues here without exposing the opaque conversation identifier.
  - The touched route test now guards both the prior English `Conversation` kicker regression and the raw conversation-id badge regression directly.
- `validation_performed`: `npm test`
  - Exit status: `0`
  - Result: `116` passing, `0` failing
- `remaining_risks`:
  - Existing `MODULE_TYPELESS_PACKAGE_JSON` warnings remain in the Node test path but did not block validation.
  - This repair intentionally did not expand into unrelated design-differentiation work outside `story-6.4`.
- `return_to_lane_notes`:
  - Repair lane stayed within the bounded review packet: reproduced the shipped badge leak first, patched only the workbench copy, direct regression coverage, and implementation artifact claims needed for this review closure.

## Selected Story

- `story-3.4` - Response restoration and transactional run completion

## Quality Plan

- `story-3.4:GOAL-1`
  Code: added `apps/desktop/src/main/workflows/response-completion.ts` to claim optimized runs, call the cloud gateway, optionally call local restore, and finish the run with a persisted assistant message.
  Test: `apps/desktop/tests/response-restoration.test.ts` covers Korean completion, English-direct completion, and restore failure.
  Artifact: this summary records the new orchestration boundary and deferred pricing/provider follow-ups.
- `story-3.4:SCOPE-1`
  Code: `response-completion.ts` stores `provider_response_en` in the successful completion transaction and in the restore-failure transaction.
  Test: `story-3.4:VAL-1` and `story-3.4:VAL-2` assert raw English artifact persistence.
- `story-3.4:SCOPE-2`
  Code: `apps/desktop/src/shared/ipc/contracts.ts` and `apps/desktop/src/main/settings/service.ts` now allow `responseLanguage: 'ko' | 'en'`, and `response-completion.ts` branches on that setting.
  Test: `story-3.4:VAL-3` drives the English setting through the persistent settings service and verifies direct-English completion.
- `story-3.4:SCOPE-3`
  Code: `response-completion.ts` commits `provider_response_en`, optional `restored_response_ko`, the assistant message, the completed stage, and the usage row in one success transaction.
  Test: `story-3.4:VAL-1` asserts the final completion state and stored records; `story-3.4:VAL-2` asserts the failure branch does not create the final assistant message or usage row.
- `story-3.4:AC-1`
  Code: Korean response mode enters a `restoring` stage and uses `translationAdapter.restoreResponse(...)` before final commit.
  Test: `story-3.4:VAL-1` verifies the final assistant message is the restored Korean output and the raw English remains inspectable.
- `story-3.4:AC-2`
  Code: English response mode skips local restore entirely and finalizes with the provider English text.
  Test: `story-3.4:VAL-3` verifies `restoreResponse` is never called and the assistant message equals the raw English response.
- `story-3.4:AC-3`
  Code: successful completion uses one transaction in `completeRun(...)`; restore failures persist only the raw English artifact and failure state.
  Test: `story-3.4:VAL-1` and source inspection confirm the success-transaction boundary; `story-3.4:VAL-2` confirms the restore-failure split.
- `story-3.4:AC-4`
  Code: restoration reuses the same preservation-hint extraction path through `apps/desktop/src/main/workflows/run-helpers.ts`.
  Test: `story-3.4:VAL-1` asserts preserved table rows, numbered list items, numeric literals, and checklist syntax in the completed Korean message.
- `story-3.4:VAL-1`
  Test: `apps/desktop/tests/response-restoration.test.ts` first case.
- `story-3.4:VAL-2`
  Test: `apps/desktop/tests/response-restoration.test.ts` second case.
- `story-3.4:VAL-3`
  Test: `apps/desktop/tests/response-restoration.test.ts` third case.

## In Scope for This Run

- Optimized-run completion orchestration from cloud English response to final persisted assistant message
- Response-language-aware Korean restoration vs English-direct finalization
- Transactional usage and completion writes required for `story-3.4`

## Deferred Work

- `story-3.5` run-stage renderer feedback and retry UX
- `story-4.1` richer pricing/version policy, explicit estimate flags, and dashboard-facing savings semantics beyond the minimum usage row needed for this story
- Dashboard/history UI changes
- Anthropic/Gemini provider adapters remain outside this slice even though the gateway seam is now wired into app boot

## Validation Performed

- `npm --workspace @talkin-ai/desktop run test -- response-restoration.test.ts`
  Result: pass, and the current package test script executed the full desktop suite with `42/42` passing.
- `npm --workspace @talkin-ai/desktop run validate:story-3.4`
  Result: pass, `42/42` tests passing.
- `npm --workspace @talkin-ai/desktop run typecheck`
  Result: fail on the pre-existing package issue `TS2688 Cannot find type definition file for 'vite/client'`.

## Implementation Summary

- Added a new response-completion orchestrator in [response-completion.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/main/workflows/response-completion.ts:125) that:
  - claims `optimized` runs into `cloud_pending`
  - calls the cloud inference gateway
  - branches on configured response language
  - enters `restoring` only for Korean output
  - commits the final assistant message and usage row transactionally on success
  - preserves `provider_response_en` even when restore fails
- Extracted shared prompt-preservation and minimum usage-record helpers into [run-helpers.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/main/workflows/run-helpers.ts:47) so optimization and restoration use the same structure-preservation hints and token-estimate path.
- Wired the real app boot path in [index.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/main/index.ts:26) so submitted chat runs now flow from local optimization into cloud inference and then into completion/restoration without renderer changes.
- Expanded settings contracts to allow English response mode in [contracts.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/shared/ipc/contracts.ts:56) and [service.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/main/settings/service.ts:17).
- Added a SQLite busy timeout in [database.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/main/persistence/database.ts:230) to stabilize concurrent CLI-backed test access after the new background completion flow.
- Added `story-3.4` coverage in [response-restoration.test.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/tests/response-restoration.test.ts:195) and a matching package script in [package.json](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/package.json:15).

## Story Assessment

- `story-3.4:GOAL-1`: pass. Optimized English responses now become either restored Korean assistant messages or English-direct assistant messages through a persisted main-process completion path.
- `story-3.4:SCOPE-1`: pass. `provider_response_en` is stored on both success and restore failure.
- `story-3.4:SCOPE-2`: pass. `responseLanguage` now drives the completion branch.
- `story-3.4:SCOPE-3`: pass. Success writes for final message, completed stage, and usage record are committed together.
- `story-3.4:AC-1`: pass. Korean-default completion stores restored Korean text.
- `story-3.4:AC-2`: pass. English completion uses raw English directly and skips local restore.
- `story-3.4:AC-3`: pass. The success path writes `provider_response_en`, the assistant message, the completed stage, and the usage row inside one transaction.
- `story-3.4:AC-4`: pass. Structured response samples keep tables, lists, numbers, and checklists through restoration.
- `story-3.4:VAL-1`: pass.
- `story-3.4:VAL-2`: pass.
- `story-3.4:VAL-3`: pass.

## Design Compliance Notes

- status: not_applicable
- Reason: this run changed main-process orchestration and tests only; no renderer layout or user-facing screen copy changed.

## Design Differentiation Notes

- status: not_applicable
- intended archetype: `chat-first inbox`
- visible choices made: none in this run
- layout silhouette difference from default/control: none in this run
- default convergence risks avoided: no renderer changes were made
- remaining weak spots: `story-3.5` still needs renderer-stage feedback for the new `cloud_pending` and `restoring` states

## Outcome Quality Notes

- purpose clarity: improved within this slice because the persisted run now reaches a real completion outcome instead of stopping at `optimized`
- primary action: unchanged in UI; a submitted Korean prompt can now complete into a final assistant message through the background loop
- state/copy handling: no renderer copy changes in this run; completion and failure states are now persisted for later surfaces to render
- visible quality choices: preserved raw English artifacts, language-aware finalization, and structure-preserving restore coverage
- remaining weak spots: package typecheck is still blocked by the unrelated `vite/client` issue, and pricing semantics are intentionally minimal until `story-4.1`

## Structured Assessment

```json
{
  "success_criteria_status": "partial",
  "story_completion_status": "completed",
  "mvp_acceptance_status": "not_met",
  "release_evidence_status": "partial",
  "package_readiness_status": "not_ready",
  "remaining_required_story_keys": [
    "story-3.5",
    "story-4.1",
    "story-4.2",
    "story-4.3",
    "story-4.4",
    "story-5.1",
    "story-5.2",
    "story-5.3",
    "story-5.4",
    "story-5.5",
    "story-5.6",
    "story-5.7",
    "story-5.8",
    "story-6.1",
    "story-6.2",
    "story-6.3",
    "story-6.4"
  ],
  "next_story_candidates": [
    "story-3.5",
    "story-4.1",
    "story-4.2"
  ],
  "risks": [
    "Desktop package typecheck still fails on the pre-existing missing vite/client type-definition issue.",
    "The real app boot path is now wired through cloud completion, but only the OpenAI adapter is implemented in code today.",
    "Usage pricing and estimate semantics are intentionally minimal until story-4.1 adds the full ledger contract."
  ]
}
```

# Selected Story

- Story Key: `story-3.1`
- Epic Key: `epic-3`
- Title: `Translation MCP adapter and local engine health model`

## Quality Plan

| Criterion ID | Intended Code/Test/Artifact Evidence |
| --- | --- |
| `story-3.1:GOAL-1` | Add a main-process Translation MCP adapter under `apps/desktop/src/main/translation` and wire it into the main-process service container path. |
| `story-3.1:SCOPE-1` | Implement `healthCheck`, `optimizePrompt`, `restoreResponse`, and `summarizeConversationContext` contracts in `apps/desktop/src/main/translation/service.ts`. |
| `story-3.1:SCOPE-2` | Provide a stdio-backed local-only runtime transport plus a fake runtime seam for tests in `apps/desktop/src/main/translation/service.ts`. |
| `story-3.1:SCOPE-3` | Define a local engine availability health-state model that returns `ready`, `degraded`, or `unavailable` with failure details. |
| `story-3.1:AC-1` | Verify the main-process service can hold and use the adapter for health, optimize, and restore calls in `apps/desktop/tests/translation-mcp.test.ts`. |
| `story-3.1:AC-2` | Ensure optimize/restore/summary inputs accept optimization mode plus preservation-related fields, and assert those payloads in automated tests. |
| `story-3.1:AC-3` | Use a stdio transport with no public listener shape and validate unavailable health when the sidecar command is absent. |
| `story-3.1:AC-4` | Export a fake runtime factory and cover adapter behavior with fully automated tests that do not require a real sidecar. |
| `story-3.1:VAL-1` | Add a fake-runtime round-trip test for health, optimize, restore, and summarize operations. |
| `story-3.1:VAL-2` | Add a test that blocks renderer-context adapter creation and confirms the renderer/preload surface does not expose translation runtime access. |
| `story-3.1:VAL-3` | Add a sidecar-down stdio test that returns a clear `unavailable` health result instead of throwing to the caller. |

## In Scope for This Run

- `apps/desktop/src/main/translation/service.ts` main-process Translation MCP adapter contract
- `apps/desktop/src/main/translation/index.ts` export surface for the adapter and runtime seams
- main-process wiring in `apps/desktop/src/main/index.ts` and `apps/desktop/src/main/ipc/register-ipc.ts`
- automated validation in `apps/desktop/tests/translation-mcp.test.ts`
- validation script aliases for `story-3.1`

## Deferred Work

- `story-3.2` orchestrator stage wiring from queued runs into the adapter
- `story-3.2` persistence of optimized prompt artifacts, preservation checks, and summarized context
- `story-6.2` settings-screen health presentation and keychain-backed local-engine runtime configuration
- any bundled local runtime packaging beyond the current pluggable stdio/local-only adapter seam

## Validation Performed

- `npm --workspace @talkin-ai/desktop run test`
  - Result: PASS
  - Evidence: all `32/32` desktop tests passed, including the new `story-3.1` coverage
- `npm --workspace @talkin-ai/desktop run validate:story-3.1`
  - Result: PASS
- `npm run validate:story-3.1`
  - Result: PASS
- `npm --workspace @talkin-ai/desktop run typecheck`
  - Result: FAIL
  - Reason: pre-existing renderer blocker `TS2688 Cannot find type definition file for 'vite/client'`
- `npm exec -- tsc -p tsconfig.electron.json --noEmit` from `apps/desktop`
  - Result: FAIL
  - Reason: pre-existing repo-wide Electron/main TypeScript issues remain, including `.ts` import-extension checks and missing `electron` type resolution; no new `story-3.1`-specific error remained after the `process.type` typing fix

## Criterion Assessment

- `story-3.1:GOAL-1` PASS: the repo now has a dedicated main-process Translation MCP adapter module plus runtime seams and main-process wiring.
- `story-3.1:SCOPE-1` PASS: `healthCheck`, `optimizePrompt`, `restoreResponse`, and `summarizeConversationContext` are implemented in `apps/desktop/src/main/translation/service.ts`.
- `story-3.1:SCOPE-2` PASS: the adapter supports a local-only stdio runtime and a fake runtime with no public network listener shape.
- `story-3.1:SCOPE-3` PASS: health checks return a structured `ready | degraded | unavailable` model with failure codes, timestamps, warnings, and latency metadata.
- `story-3.1:AC-1` PASS: the main-process desktop service can hold the adapter and use it for health, summarize, optimize, and restore calls in `apps/desktop/tests/translation-mcp.test.ts`.
- `story-3.1:AC-2` PASS: optimize/restore/summary contracts accept `mode` and explicit preservation inputs such as entities, constraints, output format, structure, and numeric preservation.
- `story-3.1:AC-3` PASS: the production runtime path is stdio-only, and sidecar-down behavior returns a clear unavailable health result rather than assuming any public listener fallback.
- `story-3.1:AC-4` PASS: `createFakeTranslationMcpRuntime(...)` enables fully automated adapter tests without a live sidecar.
- `story-3.1:VAL-1` PASS: fake-runtime tests cover health plus summarize/optimize/restore round-trip behavior.
- `story-3.1:VAL-2` PASS: renderer-context adapter creation is rejected and the preload/renderer bridge still exposes no translation runtime surface.
- `story-3.1:VAL-3` PASS: a missing stdio sidecar command maps to `status=unavailable` and `failureCode=sidecar_unreachable`.

## Design Compliance Notes

- Status: `not_applicable`
- Reason: `story-3.1` is a main-process adapter and health-model slice with no renderer UI or user-flow changes.

## Design Differentiation Notes

- Status: `not_applicable`
- Intended archetype: `chat-first inbox`
- Visible choices made: none in this run
- Layout silhouette difference from default/control: none in this run
- Default convergence risks avoided: no UI changes were made
- Remaining weak spots: user-facing engine-health presentation is deferred to later settings/workflow stories

## Outcome Quality Notes

- Purpose clarity: the main process now has a stable, product-specific seam for local Korean optimization and Korean restoration instead of leaving Translation MCP as an implied future dependency.
- Primary action: not applicable for direct user interaction in this slice; the value here is reliable main-process orchestration readiness for later queued-run execution stories.
- State/copy handling: the adapter health model improves internal state clarity by distinguishing `ready`, `degraded`, and `unavailable` instead of a single boolean or thrown error.
- Visible quality choices: preservation inputs are explicit in the contract, and sidecar-down health checks fail safely without suggesting any raw-Korean paid-cloud fallback.
- Remaining weak spots: no real orchestrator stage uses the adapter yet, and runtime command/credential sourcing is still intentionally deferred.

## Structured Assessment

```json
{
  "success_criteria_status": "partial",
  "story_completion_status": "completed",
  "mvp_acceptance_status": "partial",
  "release_evidence_status": "pass_for_story_slice",
  "package_readiness_status": "warning_preexisting_typecheck_gaps_remain",
  "remaining_required_story_keys": [
    "story-3.2",
    "story-3.3",
    "story-3.4",
    "story-3.5",
    "story-4.1",
    "story-4.2",
    "story-4.3",
    "story-4.4",
    "story-5.1",
    "story-5.2",
    "story-5.3",
    "story-5.4",
    "story-5.5",
    "story-5.6",
    "story-5.7",
    "story-5.8",
    "story-6.1",
    "story-6.2",
    "story-6.3",
    "story-6.4"
  ],
  "next_story_candidates": [
    "story-3.2",
    "story-3.3"
  ],
  "risks": [
    "The adapter is wired in the main process but not yet invoked by queued-run orchestration; that remains story-3.2 scope.",
    "The default stdio command is pluggable through TALKIN_AI_TRANSLATION_MCP_COMMAND, but keychain-backed runtime configuration and user-facing health display are still deferred.",
    "Package typecheck remains blocked by pre-existing repo issues, starting with missing vite/client typings and separate Electron/main TypeScript configuration gaps."
  ]
}
```

# Selected Story

- `story-6.3` - Restart recovery and interrupted-run safeguards

# Quality Plan

- `story-6.3:GOAL-1`
  Code: add a startup recovery workflow that inspects persisted in-flight runs and applies the safe recovery policy before the user continues.
  Test: add recovery-focused desktop tests that simulate restart after pre-dispatch interruption and post-dispatch interruption.
  Artifact: record the exact recovery policy and evidence in this summary.
- `story-6.3:SCOPE-1`
  Code: resume or requeue persisted `queued` / `optimizing` / `optimized` runs without asking the user to re-enter the Korean source prompt.
  Test: verify `queued` and `optimizing` restart recovery reaches a resumable or completed state after the recovery sweep.
- `story-6.3:SCOPE-2`
  Code: convert persisted `cloud_pending` / `restoring` runs into an explicit-retry failure state with `interrupted_after_dispatch` guidance.
  Test: verify restart recovery never auto-dispatches those runs and keeps the source message for follow-up retry.
- `story-6.3:SCOPE-3`
  Code: surface explicit-retry guidance for interrupted-after-dispatch runs in the chat run-feedback helper.
  Test: add chat-surface assertions for the new interrupted-after-dispatch copy and actions.
- `story-6.3:AC-1`
  Code: recovery sweep plus safe auto-resume for pre-dispatch runs.
  Test: simulate restart from `queued` / `optimizing` and assert the run appears resumable or finishes successfully.
- `story-6.3:AC-2`
  Code: mark post-dispatch interruptions as failed with retry guidance instead of automatic replay.
  Test: simulate restart from `cloud_pending` and assert the run lands in a failed explicit-retry state.
- `story-6.3:AC-3`
  Code: preserve the original Korean message linkage and interrupted stage details while recovering or flagging runs.
  Test: assert the source message id and stored Korean message survive restart and retry.
- `story-6.3:AC-4`
  Code: distinguish automatic continuation from confirm-before-retry in user-facing feedback copy.
  Test: assert chat feedback describes explicit retry for interrupted-after-dispatch runs.
- `story-6.3:VAL-1`
  Planned command: `npm --workspace @talkin-ai/desktop run validate:story-6.3`
  Evidence target: restart simulation for `queued` / `optimizing`.
- `story-6.3:VAL-2`
  Planned command: `npm --workspace @talkin-ai/desktop run validate:story-6.3`
  Evidence target: restart simulation for interrupted-after-dispatch handling.
- `story-6.3:VAL-3`
  Planned command: `npm --workspace @talkin-ai/desktop run validate:story-6.3`
  Evidence target: retry from an interrupted run reuses the same source message.

# In Scope for This Run

- Add persisted restart-recovery handling for in-flight chat runs.
- Resume safe pre-dispatch runs and flag unsafe post-dispatch runs for explicit retry.
- Update chat feedback copy/tests so interrupted-after-dispatch runs are understandable in the UI.
- Add `validate:story-6.3` coverage and refresh this implementation summary.

# Deferred Work

- `story-6.4` cross-screen projection sync hardening remains out of scope.
- Any new history-detail UI for stage-by-stage recovery inspection remains deferred unless current acceptance requires it.
- Guided visual verification is deferred unless the implemented UI copy changes expose a new route-level issue.

# Validation Performed

- `npm --workspace @talkin-ai/desktop run validate:story-6.3`
  Result: PASS
  Evidence:
  - `story-6.3:VAL-1` passed through `apps/desktop/tests/restart-recovery.test.ts`, covering queued and optimizing restart recovery.
  - `story-6.3:VAL-2` passed through `apps/desktop/tests/restart-recovery.test.ts`, covering `interrupted_after_dispatch` handling for persisted `cloud_pending`.
  - `story-6.3:VAL-3` passed through `apps/desktop/tests/restart-recovery.test.ts`, covering retry reuse of the same Korean source message after interruption.
  - Full desktop suite result during this run: `110` passing, `0` failing.
- `npm --workspace @talkin-ai/desktop run typecheck`
  Result: FAIL
  Note: unchanged workspace/tooling blocker `TS2688 Cannot find type definition file for 'vite/client'`.

# Implementation Summary

- Added [restart-recovery.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/main/workflows/restart-recovery.ts) to scan persisted in-flight runs on startup.
- Safe pre-dispatch recovery:
  - persisted `queued` runs are resumed through the existing optimization orchestrator.
  - persisted `optimizing` runs are re-queued with a `restart-recovery` stage marker and then resumed automatically.
  - persisted `optimized` runs resume the cloud/restore completion path from the stored `optimized_prompt_en` artifact.
- Unsafe post-dispatch recovery:
  - persisted `cloud_pending` and `restoring` runs are converted to `failed` with `errorCode=interrupted_after_dispatch`.
  - the failure record preserves the interrupted stage, keeps the Korean source message linkage, and forces an explicit retry path instead of silent replay.
- Wired startup recovery into [index.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/main/index.ts) so the desktop main process runs the recovery sweep once at boot.
- Updated [chat-surface.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/renderer/routes/chat-surface.ts) so interrupted-after-dispatch runs explain why auto-resume was skipped and present explicit retry actions.
- Added `validate:story-6.3` scripts in [package.json](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/package.json) and [apps/desktop/package.json](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/package.json).

# Story Assessment

- `story-6.3:GOAL-1`
  Result: implemented. Startup recovery now preserves persisted work and routes it through the safe recovery policy instead of leaving stale in-flight runs behind.
- `story-6.3:SCOPE-1`
  Result: implemented. `queued`, `optimizing`, and `optimized` runs are recoverable without re-entering the Korean prompt.
- `story-6.3:SCOPE-2`
  Result: implemented. post-dispatch interruptions are normalized to `interrupted_after_dispatch` and require explicit retry.
- `story-6.3:SCOPE-3`
  Result: implemented. chat feedback now explains the explicit retry rule in user-facing Korean copy.
- `story-6.3:AC-1`
  Result: pass. restart recovery tests show queued and optimizing runs resume to completion.
- `story-6.3:AC-2`
  Result: pass. restart recovery tests show `cloud_pending` runs are not replayed automatically.
- `story-6.3:AC-3`
  Result: pass. interrupted and retried runs retain the original Korean message linkage and interrupted-stage details.
- `story-6.3:AC-4`
  Result: pass. interrupted-after-dispatch chat feedback distinguishes auto-continued work from confirm-before-retry work.

# Design Compliance Notes

- Status: pass
- Relevant constraints preserved:
  - Korean remains the primary user language for restart feedback.
  - Recovery behavior stays inside the existing chat-first workspace flow instead of exposing developer-style recovery controls.
  - Explicit retry copy is concise and task-oriented rather than technical.
- Weak spots:
  - This run changed feedback copy and recovery state only; it did not add new visual evidence for the route.

# Design Differentiation Notes

- Status: pass
- Intended archetype: chat-first inbox
- Visible choices made: interrupted runs explain the next action inside the existing conversation status card instead of diverting the user to a modal or admin-style warning panel.
- Layout silhouette difference from default/control: unchanged; the feature stays inside the existing run-status card in the chat inbox rather than adding a generic error dashboard.
- Default convergence risks avoided: avoided cold technical error language by using Korean task guidance tied to retry decisions.
- Remaining weak spots: no new live screenshot evidence was captured in this run because the story only changed restart-state behavior and copy.

# Outcome Quality Notes

- Purpose clarity: when a restarted run may already have reached the provider, the user now gets a concrete explanation that auto-replay was skipped.
- Primary action: the safe next action is explicit retry from the same Korean source message.
- State/copy handling: pre-dispatch runs continue automatically; post-dispatch runs stop with preserved context and clear retry guidance.
- Visible quality choices: preserved the original Korean prompt, preserved interrupted stage metadata, and kept the retry path inside the same conversation workflow.
- Remaining weak spots: workspace `typecheck` is still blocked by the unrelated `vite/client` environment issue, and no live visual capture was added for this story.

# Structured Assessment

```json
{
  "success_criteria_status": "not_met",
  "story_completion_status": "completed",
  "mvp_acceptance_status": "not_met",
  "release_evidence_status": "partial",
  "package_readiness_status": "not_ready",
  "remaining_required_story_keys": [
    "story-6.4"
  ],
  "next_story_candidates": [
    "story-6.4"
  ],
  "risks": [
    "Package typecheck still fails on the existing vite/client environment issue.",
    "Epic-6 live visual evidence for prior settings work remains a separate guided-verification item outside story-6.3.",
    "Restart recovery preserves and resumes the current single-device desktop flow only; multi-device sync remains out of scope."
  ]
}
```

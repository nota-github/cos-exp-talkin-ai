# Selected Story

- Story Key: `story-4.1`
- Title: `Token estimation and pricing ledger per run`

## Quality Plan

| Criterion ID | Intended Evidence |
| --- | --- |
| `story-4.1:GOAL-1` | Persist per-run baseline/optimized token counts and savings math in `usage_records`, produced from the completed-run workflow. |
| `story-4.1:SCOPE-1` | Keep baseline Korean token estimation and optimized English token estimation inside `apps/desktop/src/main/workflows/run-helpers.ts` on the same model pricing basis. |
| `story-4.1:SCOPE-2` | Use provider-reported usage when present, fall back to local estimates when absent, and persist an estimate flag on the usage ledger row. |
| `story-4.1:SCOPE-3` | Persist pricing table version alongside per-run cost-with and cost-without-optimization values so savings remain reproducible. |
| `story-4.1:AC-1` | Verify completed runs write baseline input tokens, optimized input tokens, output tokens, pricing version, latency, and estimate status into `usage_records`. |
| `story-4.1:AC-2` | Validate cost comparison math uses the same provider/model pricing table through direct unit coverage of `buildUsageRecordInput(...)`. |
| `story-4.1:AC-3` | Add persistence and orchestration coverage showing missing provider usage stores an estimate-marked ledger row for later UI labeling. |
| `story-4.1:AC-4` | Preserve versioned pricing strings in persisted rows and cover the desktop-schema migration/backfill path for older databases. |
| `story-4.1:VAL-1` | Add a sample pricing/run test that checks per-run savings math and stored pricing version. |
| `story-4.1:VAL-2` | Cover both provider-usage-present and provider-usage-absent completed-run cases, including the persisted estimate flag. |
| `story-4.1:VAL-3` | Add a guard test that rejects mixed provider/model pricing comparisons. |

## In Scope for This Run

- `usage_records` persistence for `story-4.1`
- Completed-run ledger generation and estimate-flag storage
- SQLite schema support for the new ledger flag
- Automated validation for savings math and provider-usage present/absent cases
- Added `is_estimated` to the usage ledger schema and repository mapping so completed runs can distinguish estimate-backed rows from provider-backed rows
- Kept savings math inside `buildUsageRecordInput(...)` while persisting versioned pricing and latency alongside token counts
- Added regression coverage for legacy desktop DB migration, mixed provider/model guards, and provider-usage fallback behavior

## Deferred Work

- `story-4.2` request-level metadata presentation in the renderer
- `story-4.3` aggregate usage dashboard queries and visualization
- `story-4.4` history inspection UI over the persisted ledger and prompt artifacts
- Price-table editing or external billing reconciliation flows

## Validation Performed

- `node --experimental-strip-types --import ./tests/register-ts-loader.mjs --test tests/usage-ledger.test.ts tests/response-restoration.test.ts` PASS
  - Verified `story-4.1:VAL-1` with direct savings math assertions
  - Verified `story-4.1:VAL-2` with a completed run that persists an estimate-marked ledger row when provider usage is absent
  - Reconfirmed completed-run persistence behavior from the response-completion path
- `npm test` PASS
  - Workspace desktop suite passed `51/51`
  - Covers `story-4.1:VAL-1`, `story-4.1:VAL-2`, `story-4.1:VAL-3`, the schema migration/backfill path for `story-4.1:AC-4`, and no regressions across existing desktop stories

## Design Compliance Notes

- `not_applicable` because `story-4.1` does not change renderer UI or user-facing flow in this run

## Design Differentiation Notes

- status: `not_applicable`
- intended archetype: `chat-first inbox`
- visible choices made: none; renderer was unchanged in this story slice
- layout silhouette difference from default/control: none
- default convergence risks avoided: none; no UI changes
- remaining weak spots: downstream UI stories still need to present estimate-vs-actual savings clearly

## Outcome Quality Notes

- purpose clarity: this slice focuses on trustworthy persisted savings evidence behind later History and Usage surfaces
- primary action: not applicable to renderer interaction; the main contract is completed-run ledger persistence
- state/copy handling: not applicable because no user-facing copy changed
- visible quality choices: versioned pricing and estimate marking are stored on the same usage row so later UI can explain `예상 절감` vs provider-backed rows without reconstructing hidden workflow state
- remaining weak spots: no renderer metadata or dashboard view is included in `story-4.1`, so the persisted estimate flag is not yet surfaced to end users

## Structured Assessment

```json
{
  "success_criteria_status": "partial",
  "story_completion_status": "complete",
  "mvp_acceptance_status": "partial",
  "release_evidence_status": "pass",
  "package_readiness_status": "validated_for_story_slice",
  "remaining_required_story_keys": [
    "story-4.2",
    "story-4.3",
    "story-4.4"
  ],
  "next_story_candidates": [
    "story-4.2",
    "story-4.3"
  ],
  "risks": [
    "The persisted estimate flag is backend-only until `story-4.2` surfaces it as user-facing metadata.",
    "Savings still rely on the current local token estimator rather than provider-specific tokenizers, so future pricing-accuracy work may still tighten estimates without changing this story contract."
  ]
}
```

# Selected Story

- Story Key: `story-4.2`
- Title: `Request-level result metadata presentation`

## Quality Plan

| Criterion ID | Intended Evidence |
| --- | --- |
| `story-4.2:GOAL-1` | Add a compact response metadata row under completed assistant messages showing model, latency, savings wording, and a workbench continuation action. |
| `story-4.2:SCOPE-1` | Extend chat feed run summaries with persisted per-run usage metadata and render compact metadata chips in the chat response surface. |
| `story-4.2:SCOPE-2` | Wire a response-level `작업대에서 계속하기` action from the chat response surface to the existing `openInWorkbench` IPC command. |
| `story-4.2:SCOPE-3` | Distinguish estimated savings with user-facing copy such as `예상 절감` instead of presenting it as exact savings. |
| `story-4.2:AC-1` | Completed responses render compact model, latency, and savings metadata from persisted run usage in renderer code and supporting tests. |
| `story-4.2:AC-2` | Estimated usage rows produce `예상 절감` labeling in renderer helpers and automated tests. |
| `story-4.2:AC-3` | Response metadata action calls `openInWorkbench` with the current task id and navigates to the workbench route; cover with automated tests. |
| `story-4.2:AC-4` | In-progress and failed runs keep stage/error feedback instead of completed metadata; preserve the existing run feedback card and add tests for the rendering split. |
| `story-4.2:AC-5` | Keep metadata visual treatment compact and secondary through small inline chips/row styling and source assertions. |
| `story-4.2:AC-6` | Use a restrained mint-accent chip for savings only, verified in CSS/source inspection tests. |
| `story-4.2:VAL-1` | Update chat surface tests to verify completed metadata output versus failed/in-progress feedback behavior. |
| `story-4.2:VAL-2` | Add a helper test that verifies `작업대에서 계속하기` forwards the same task id to `openInWorkbench`. |
| `story-4.2:VAL-3` | Add metadata-label tests for estimated versus non-estimated savings. |
| `design-must-have-5` | Preserve the chat-first route as the primary screen while adding metadata below responses rather than moving it into a dashboard panel. |
| `design-must-have-9` | Keep the metadata inside the natural chat response flow so the chat UI remains the main interaction surface. |
| `design-reference-1` | Reflect the Korean-first optimization engine through savings wording that explains optimization value without exposing raw developer diagnostics. |
| `design-layout-1` | Keep aggregate usage/dashboard work deferred; only surface request-level evidence in the chat response flow for this story. |

## In Scope for This Run

- Persisted chat feed run summaries now include per-run usage metadata needed for response-level presentation.
- Completed assistant responses render a compact metadata row with model, latency, savings wording, and a response-level `작업대에서 계속하기` action.
- In-progress and failed runs keep the existing stage/error feedback card instead of reusing completed metadata styling.
- Added renderer and workflow tests that verify exact-vs-estimated savings labels, task-id handoff to workbench, and feed query exposure of completed/failed run metadata.

## Deferred Work

- Aggregate usage dashboard changes from later `story-4.x` slices
- Workbench layout redesign outside the response-level handoff entrypoint
- Rich multi-task routing rules or panel-slot selection logic for the workbench handoff action
- Any wording or product decision for negative savings display beyond the safe `0%` clamp used to avoid overclaiming request-level savings

## Validation Performed

- `npm test` PASS
  - Covers `story-4.2:VAL-1`, `story-4.2:VAL-2`, and `story-4.2:VAL-3` through updated `chat-surface`, `response-restoration`, and `retry-run` tests.
  - Confirms completed responses produce compact metadata, estimated rows say `예상 절감`, failed runs keep stage/error feedback, and workbench continuation forwards the same task id.
- `npm run typecheck` BLOCKED
  - Existing environment issue remains unchanged: `TS2688 Cannot find type definition file for 'vite/client'`.
- Browser verification ATTEMPTED but GUIDED_VERIFICATION_REQUIRED
  - The Browser plugin session could not attach to an in-app browser target and returned `Browser is not available: iab`, so no visual browser proof was captured in this run.

## Implementation Summary

- Extended `ChatFeedResult` with a `runs` collection and optional per-run `usage` summaries in [contracts.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/shared/ipc/contracts.ts:88), then populated that data from persisted `usage_records` in [service.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/main/chat/service.ts:218).
- Added compact response metadata helpers and a workbench handoff helper in [chat-surface.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/renderer/routes/chat-surface.ts:42), then rendered the metadata row only under completed assistant bubbles in [ChatRoute.tsx](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/renderer/routes/ChatRoute.tsx:281).
- Kept non-completed runs on the existing stage/error card path by suppressing the verbose completed-success card and retaining progress/failure feedback for active runs in [ChatRoute.tsx](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/renderer/routes/ChatRoute.tsx:430).
- Added restrained row styling in [styles.css](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/renderer/styles.css:555) so mint accent is limited to the savings chip and remains secondary to the response body.
- Updated the fake IPC seed in [register-ipc.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/main/ipc/register-ipc.ts:208) so the contract shape stays aligned outside the persistent main-process path.

## Acceptance / Success Criteria Mapping

- `story-4.2:GOAL-1` PASS
  - Completed responses now expose model, latency, savings wording, and a workbench continuation action in the response flow.
- `story-4.2:SCOPE-1` PASS
  - Compact response metadata is rendered from persisted run usage summaries.
- `story-4.2:SCOPE-2` PASS
  - `작업대에서 계속하기` is wired to `openInWorkbench`.
- `story-4.2:SCOPE-3` PASS
  - Estimated rows display `예상 절감`.
- `story-4.2:AC-1` PASS
  - Completed responses show model, latency, and savings metadata through `getChatResponseMetadata(...)`.
- `story-4.2:AC-2` PASS
  - Estimated savings wording is explicitly differentiated in tests and renderer helpers.
- `story-4.2:AC-3` PASS
  - Workbench continuation sends the existing `taskId` and navigates to `/workbench`.
- `story-4.2:AC-4` PASS
  - Failed or in-flight runs stay on stage/error feedback instead of completed metadata styling.
- `story-4.2:AC-5` PASS
  - Metadata uses a compact inline row below the answer rather than a verbose log panel.
- `story-4.2:AC-6` PASS
  - Mint accent is applied only to the savings chip via subtle background and border styling.
- `story-4.2:VAL-1` PASS
  - Automated tests cover completed metadata vs failed/progress feedback split.
- `story-4.2:VAL-2` PASS
  - Automated tests verify same-task handoff to workbench.
- `story-4.2:VAL-3` PASS
  - Automated tests verify estimated vs non-estimated wording.

## Risks and Open Questions

- The Browser plugin could not attach to an in-app browser target in this session, so visual proof of the final row placement is still missing even though source and automated tests align.
- Request-level savings are intentionally clamped to `0%` when persisted token math would otherwise go negative, to avoid overclaiming savings. If product wants a distinct `절감 없음` or negative-delta treatment later, that should be a separate UX decision.

## Design Compliance Notes

- `design-must-have-5`: `pass` because the change stays inside the `Chat` route and keeps request result evidence directly in the chat flow.
- `design-must-have-9`: `pass` because metadata is attached under completed responses instead of moving into a separate dashboard or inspector surface.
- `design-reference-1`: `pass` because the row explains optimization value in Korean-first wording without exposing raw internal prompt plumbing.
- `design-layout-1`: `pass` because aggregate dashboard work remains deferred and this slice only adds request-level evidence where the story asked for it.

## Design Differentiation Notes

- status: `pass`
- intended archetype: `chat-first inbox`
- visible choices made: metadata now sits as a quiet inline row under assistant answers, and the workbench continuation action is phrased as part of the ongoing chat workflow.
- layout silhouette difference from default/control: preserved the existing inbox split and kept the new evidence embedded inside message bubbles rather than adding a generic stats bar or inspector rail.
- default convergence risks avoided: avoided a cold dashboard treatment by using compact Korean labels and a restrained mint-only savings accent.
- remaining weak spots: browser-level visual evidence is still missing because the in-app browser target was unavailable in this run.

## Outcome Quality Notes

- purpose clarity: each completed answer now visibly explains which model answered, how long it took, and what the request-level optimization result was.
- primary action: the main chat action remains sending a Korean request; the secondary action after value is `작업대에서 계속하기`.
- state/copy handling: completed runs show compact evidence, estimated rows say `예상 절감`, and non-completed runs stay on user-facing progress/error copy.
- visible quality choices: kept metadata subordinate to the answer body, used mint only for savings, and retained response-level continuity into the workbench.
- remaining weak spots: no browser screenshot or live desktop capture was possible in this session, and typecheck is still blocked by the pre-existing `vite/client` environment issue.

## Structured Assessment

```json
{
  "success_criteria_status": "partial",
  "story_completion_status": "implemented_with_guided_browser_verification_required",
  "mvp_acceptance_status": "partial",
  "release_evidence_status": "automated_validation_passed_visual_browser_evidence_missing",
  "package_readiness_status": "story_slice_ready_with_existing_typecheck_environment_blocker",
  "remaining_required_story_keys": [
    "story-4.3",
    "story-4.4",
    "story-5.1",
    "story-5.2",
    "story-5.3",
    "story-5.4",
    "story-5.5",
    "story-5.6",
    "story-5.7",
    "story-5.8",
    "story-6.1",
    "story-6.2",
    "story-6.3",
    "story-6.4"
  ],
  "next_story_candidates": [
    "story-4.3",
    "story-5.1"
  ],
  "risks": [
    "Visual browser verification for the response metadata row is still missing because the Browser plugin target was unavailable in this session.",
    "Workspace typecheck remains blocked by the pre-existing TS2688 vite/client environment issue."
  ]
}
```

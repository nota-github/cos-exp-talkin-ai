# Selected Story

- Story Key: `story-5.7`
- Title: `Project detail hub and in-project task search`

## Quality Plan

| Criterion ID | Intended Evidence |
| --- | --- |
| `story-5.7:GOAL-1` | Extend the selected-project surface so one screen shows project-linked tasks, a files section, and a recent conversation/activity hub. |
| `story-5.7:SCOPE-1` | Add a selected-project detail-hub layer inside `/projects` instead of leaving the center stage as editor-only. |
| `story-5.7:SCOPE-2` | Expose richer project-detail task metadata and add renderer-side in-project task search/filtering. |
| `story-5.7:SCOPE-3` | Add recent activity / conversation-context content driven by project detail data, not static placeholder copy. |
| `story-5.7:AC-1` | Update persistence, main-process mapping, IPC fallback state, and `/projects` UI so task, file, and context/activity sections render together. |
| `story-5.7:AC-2` | Add in-project task search over linked tasks and cover it with automated tests using multiple linked tasks. |
| `story-5.7:AC-3` | Add task-open actions from project detail into workbench and detailed chat flows. |
| `story-5.7:AC-4` | Provide a file-empty state that explains the next safe action even before attachment flows land. |
| `story-5.7:AC-5` | Make the selected-project area read like a long-running context hub, not a subordinate list or table. |
| `story-5.7:AC-6` | Visually separate linked-task, file, and context sections with distinct headers, cards, and layout groupings. |
| `story-5.7:VAL-1` | Add automated validation for rich project-detail data plus linked-task filtering/search behavior. |
| `story-5.7:VAL-2` | Add automated validation for project-detail task open actions into workbench and chat flows. |
| `story-5.7:VAL-3` | Add automated evidence for file-present and file-empty project detail states; record live visual verification separately if the renderer cannot launch. |
| `design-must-have-7` | Preserve `/projects` as the global project-management destination in the workspace. |
| `design-reference-1` | Keep the hub framed as a Korean-first long-running AI workspace rather than an admin detail form. |
| `design-must-have-3` | Preserve the approved major screen structure by enhancing the project screen rather than inventing a new detached route. |

## In Scope for This Run

- Expanded project detail data contracts so linked tasks now carry:
  - `sourceScreen`
  - `summary`
  - `conversationId`
- Changed project detail files from plain strings to display-ready file objects with `fileId`, `displayName`, `mimeType`, and `sizeBytes`.
- Added `recentActivity` to project detail results, derived from the most recent linked tasks and their stored conversation summaries.
- Reworked the selected-project stage in `/projects` to include:
  - a project detail hero
  - linked-task search and action cards
  - a file section with empty and populated states
  - a recent conversation/activity section
- Preserved the existing `story-5.6` project editor and task-linking rail so neighboring project-management flows still work.
- Added automated validation for persistence/detail data, task filtering, open actions, and file-empty/file-present surface evidence.

## Deferred Work

- `story-5.8` file attachment, unlink, and managed-storage actions remain out of scope; the new file section is read-only.
- No OCR, parsing, semantic indexing, or document preview behavior was added.
- Live browser/screenshot verification of the actual `/projects` route remains `GUIDED_VERIFICATION_REQUIRED` because the local renderer cannot be launched in this workspace.
- The existing desktop typecheck environment issue (`TS2688` for `vite/client`) remains outside this story slice.

## Validation Performed

- `npm test`
  - PASS
  - `93` tests passed, including the new `story-5.7:VAL-1`, `story-5.7:VAL-2`, and `story-5.7:VAL-3` coverage in `apps/desktop/tests/projects-route.test.ts`.
- `npm --workspace @talkin-ai/desktop run typecheck`
  - FAIL
  - Existing environment issue: `TS2688: Cannot find type definition file for 'vite/client'.`
- `npm --workspace @talkin-ai/desktop run dev:renderer -- --host 127.0.0.1 --port 4173`
  - FAIL
  - Local runtime blocker: `vite: command not found`, so no live `/projects` browser verification or screenshots could be captured.

## Implementation Summary

- Updated `apps/desktop/src/shared/ipc/contracts.ts` so `getProjectDetail` now returns richer task metadata, structured files, and recent activity items.
- Extended `apps/desktop/src/main/persistence/types.ts` and `apps/desktop/src/main/persistence/repositories.ts` so project detail tasks include `sourceScreen`, latest `conversationId`, and latest stored conversation `summary`.
- Updated `apps/desktop/src/main/projects/service.ts` to map structured file summaries and derive recent project activity from linked tasks.
- Updated `apps/desktop/src/main/ipc/register-ipc.ts` so in-memory fallback state and preview data match the persistent project-detail contract.
- Extended `apps/desktop/src/renderer/routes/projects-surface.ts` with:
  - preview project detail data for file-present and file-empty states
  - `filterProjectTasks`
  - file-size formatting
  - project-detail open-action helpers
  - source-screen labels for linked tasks
- Reworked `apps/desktop/src/renderer/routes/ProjectsRoute.tsx` so the selected-project area now behaves as a detail hub with:
  - linked-task search
  - workbench/chat open actions
  - file section
  - recent conversation/context section
- Added styling in `apps/desktop/src/renderer/styles.css` to give the selected-project hub a distinct desktop workspace silhouette instead of a generic form/list layout.
- Updated `apps/desktop/tests/work-management-persistence.test.ts` and `apps/desktop/tests/projects-route.test.ts` to cover the richer detail contract and the new story-specific renderer behaviors.

## Criterion Assessment

- `story-5.7:GOAL-1` PASS
  - The selected project now surfaces linked tasks, files, and recent conversation/activity context on one screen.
- `story-5.7:SCOPE-1` PASS
  - `/projects` now includes a dedicated selected-project detail-hub layer instead of editor-only center content.
- `story-5.7:SCOPE-2` PASS
  - Linked tasks can be filtered by title, stored summary, status label, and source-screen label.
- `story-5.7:SCOPE-3` PASS
  - Recent activity/context is derived from linked tasks and their latest stored conversation summaries.
- `story-5.7:AC-1` PASS
  - Project detail now returns and renders task, file, and context/activity sections together.
- `story-5.7:AC-2` PASS
  - In-project task search is implemented and covered by automated tests with multiple linked tasks.
- `story-5.7:AC-3` PASS
  - Linked-task cards and recent-activity cards can open the same task in workbench or detailed chat flows.
- `story-5.7:AC-4` PASS
  - The file-empty state explains what the user can do next instead of ending in a dead section.
- `story-5.7:AC-5` PASS by source; `GUIDED_VERIFICATION_REQUIRED` for live route evidence
  - The selected-project area now reads as a long-running hub in source and automated evidence, but no live browser verification could run because the renderer was not launchable.
- `story-5.7:AC-6` PASS by source
  - Linked-task, file, and context areas are visually separated with different headers, card language, and layout zones.
- `story-5.7:VAL-1` PASS
  - Automated tests now verify rich detail data plus linked-task search behavior on multi-task seed data.
- `story-5.7:VAL-2` PASS
  - Automated tests verify the project-detail task open actions into workbench and chat.
- `story-5.7:VAL-3` PASS by automated state/source evidence with `GUIDED_VERIFICATION_REQUIRED` for live browser evidence
  - Both file-present and file-empty states are now covered by source/preview assertions, but no runnable renderer was available for screenshots.

## Design Compliance Notes

- `design-must-have-7`: `pass`
  - The work stays inside the existing `/projects` workspace destination.
- `design-reference-1`: `pass`
  - Copy and structure keep the screen focused on Korean-first long-running AI work rather than technical internals.
- `design-must-have-3`: `pass`
  - The project screen remains one of the approved major workspace surfaces; this run deepens the selected-project stage instead of replacing the route structure.

## Design Differentiation Notes

- status: `pass_by_source_with_guided_visual_verification_pending`
- intended archetype: `chat-first inbox`
- visible choices made: the selected-project stage now reads like an active hub with a searchable task lane, a file shelf, and a recent-context rail rather than a flat settings form.
- layout silhouette difference from default/control: the center stage changed from a simple editor block into a stacked hub with a hero, searchable task cards, and a sidecar context/file column; it no longer reads like a subordinate table under the project list.
- default convergence risks avoided: avoided a cold SaaS detail page by using task-flow language, Korean workspace copy, and explicit continue-in-workbench / continue-in-chat actions.
- remaining weak spots: no live visual proof could be captured because `vite` is unavailable in this workspace, so final differentiation judgment is still source-backed.

## Outcome Quality Notes

- purpose clarity: the selected-project area now immediately communicates that a project is a long-running AI work hub with tasks, files, and preserved context.
- primary action: the main action is now project-linked task continuation, supported by search and direct open actions.
- state/copy handling: creating, unselected, loading, error, file-empty, task-empty, and search-empty states all have explicit user-facing Korean copy.
- visible quality choices: the hub preserves existing editor/link flows while adding a stronger detail layer, which makes `/projects` feel more like a true workspace than a management form.
- remaining weak spots: file management remains read-only until `story-5.8`, and live browser verification is blocked by missing local Vite runtime support.

## Structured Assessment

```json
{
  "success_criteria_status": "story-5.7 functional pass within selected slice; live `/projects` visual verification still guided-verification-only",
  "story_completion_status": "implemented_with_guided_verification_required_for_live_ui",
  "mvp_acceptance_status": "selected_story_pass",
  "release_evidence_status": "authoritative_test_validation_passed; live_ui_browser_evidence_blocked_by_missing_vite",
  "package_readiness_status": "tests_pass; typecheck_and_renderer_launch_blocked_by_existing_vite_environment_issue",
  "remaining_required_story_keys": [
    "story-5.8",
    "story-6.1",
    "story-6.4"
  ],
  "next_story_candidates": [
    "story-5.8",
    "story-6.1"
  ],
  "risks": [
    "Live `/projects` browser verification and screenshots could not run because `npm --workspace @talkin-ai/desktop run dev:renderer` fails with `vite: command not found` in this workspace.",
    "Desktop typecheck still fails before app code analysis with `TS2688: Cannot find type definition file for 'vite/client'.`",
    "The new file section is intentionally read-only until `story-5.8` adds attachment and unlink flows.",
    "Design differentiation and outcome quality are source-backed but not yet visually verified in a runnable desktop renderer."
  ]
}
```

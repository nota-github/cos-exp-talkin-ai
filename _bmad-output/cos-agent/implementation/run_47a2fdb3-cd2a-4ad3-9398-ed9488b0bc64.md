## Selected Story

- Story Key: `story-6.1`
- Epic Key: `epic-6`
- Title: `Settings screen for preferences and advanced preview defaults`

## Quality Plan

- `story-6.1:GOAL-1`
  - Code: implement a single settings surface for non-secret app preferences with grouped user-facing sections.
  - Evidence: renderer settings route source, shared settings controller/provider, implementation summary.
- `story-6.1:SCOPE-1`
  - Code: add UI controls for `defaultModel`, `optimizationMode`, `responseLanguage`, `theme`, and `advancedPromptPreview`.
  - Test: source/assertion coverage for grouped settings UI and option wiring.
- `story-6.1:SCOPE-2`
  - Code: wire settings reads/writes through existing `getSettings` / `updateSettings` IPC and propagate theme/history-preview defaults without restart.
  - Test: persistence restart coverage in settings persistence tests; history/theme helper tests.
- `story-6.1:SCOPE-3`
  - Code: add loading, preview/empty fallback, safe read-error messaging, success, and save-error feedback with retry without presenting preview defaults as saved settings during loading or read failure.
  - Test: settings surface helper tests plus route source assertions for retry, safe copy, and control gating by surface state.
- `story-6.1:AC-1`
  - Code: each preference is changeable from the settings route.
  - Test: persistence test updates all five settings; settings route source asserts all grouped controls.
- `story-6.1:AC-2`
  - Test: restart-persistence coverage via persistent settings service and IPC round-trip.
- `story-6.1:AC-3`
  - Code: apply theme preference to the renderer shell immediately after settings change; use advanced preview preference as the default history reveal state.
  - Test: theme application helper test and history default visibility helper test.
- `story-6.1:AC-4`
  - Code: preserve previous committed settings on failed save, show safe error text, and expose retry using the last failed patch.
  - Test: settings submit helper test for failure and retry metadata; route source assertions for retry CTA.
- `story-6.1:AC-5`
  - Code: keep the screen grouped into understandable sections for model defaults, optimization, response/display, and API-key context without turning it into a dense developer panel.
  - Test: source assertions for grouped section copy and layout hooks; live verification is recorded as `GUIDED_VERIFICATION_REQUIRED` when browser/runtime tooling is unavailable.
- `story-6.1:VAL-1`
  - Validation: run desktop tests covering settings persistence across service restart after changing the full preference set.
- `story-6.1:VAL-2`
  - Validation: automated helper/source coverage that `advancedPromptPreview` changes the default history reveal state.
- `story-6.1:VAL-3`
  - Validation: automated helper/source coverage that theme application changes renderer theme attributes immediately after selection.

## In Scope for This Run

- Settings route UI for non-secret preferences only.
- Shared renderer settings state needed to apply theme and history-preview defaults.
- Story-scoped automated tests and validation script updates.

## Deferred Work

- `story-6.2` provider API key entry and connection health checks.
- `story-6.3` restart recovery UX.
- `story-6.4` broader cross-screen projection hardening beyond the settings-driven sync needed here.
- `story-6.1:AC-5` live settings-screen screenshot evidence remains `GUIDED_VERIFICATION_REQUIRED`. Browser verification was blocked in this session because the Browser plugin exposed no in-app target (`agent.browsers.list() -> []`, `agent.browsers.get("iab") -> Browser is not available: iab`), and the local renderer launch path still fails before a reachable `/settings` surface is available.

## Validation Performed

- `npm --workspace @talkin-ai/desktop run test`
  - PASS
  - Result: `100` passing, `0` failing
  - Story evidence covered:
    - `story-6.1:VAL-1` via persistent settings restart coverage in [settings-keychain.test.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/tests/settings-keychain.test.ts:65)
    - `story-6.1:SCOPE-3`, `story-6.1:VAL-2`, `story-6.1:VAL-3`, `story-6.1:AC-4`, `story-6.1:AC-5` via [settings-route.test.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/tests/settings-route.test.ts:32)
- `npm --workspace @talkin-ai/desktop run typecheck`
  - FAIL
  - Existing blocker only: `TS2688 Cannot find type definition file for 'vite/client'`
  - No new story-6.1-specific type error was surfaced before that pre-existing toolchain failure.
- `npm --workspace @talkin-ai/desktop run dev:renderer -- --host 127.0.0.1 --port 4173 --strictPort`
  - FAIL
  - Existing launcher blocker: `sh: vite: command not found`
- `npm --workspace @talkin-ai/desktop exec vite --version`
  - PASS
  - Result: `11.16.0`
- `curl -I --max-time 5 http://127.0.0.1:4173/settings`
  - FAIL
  - No local listener was reachable on `127.0.0.1:4173`, so live `/settings` capture could not proceed.
- Browser verification attempt via Browser plugin skill
  - BLOCKED
  - `agent.browsers.list()` returned `[]`
  - `agent.browsers.get("iab")` returned `Browser is not available: iab`

## Implementation Summary

- Hardened the renderer settings provider in [renderer-settings.tsx](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/renderer/app/renderer-settings.tsx:48) so unresolved desktop reads now surface `null` instead of silently falling back to preview defaults. Preview defaults are only used when the desktop bridge is unavailable, and theme application now waits for a real resolved setting before mutating the document shell.
- Tightened the `/settings` route in [SettingsRoute.tsx](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/renderer/routes/SettingsRoute.tsx:101) so loading, read-failure, empty, and preview each render distinct hero/state messaging. Loading and error states no longer imply preview values are the user’s saved defaults, the read-failure card stays on safe user guidance, and interactive controls only render in `ready` or `preview` states.
- Added explicit state helpers in [settings-surface.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/renderer/routes/settings-surface.ts:151) for hero copy, badge state, and control visibility. These helpers centralize the story-6.1 surface contract instead of leaving state-specific wording scattered across the route.
- Kept the usage/history default visibility wiring null-safe in [UsageRoute.tsx](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/src/renderer/routes/UsageRoute.tsx:115), so `advancedPromptPreview` still applies immediately once settings resolve without assuming a loaded settings object during intermediate states.
- Expanded story-scoped automated coverage in [settings-route.test.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/tests/settings-route.test.ts:32) to assert the new loading/read-failure behavior, safe error copy, and route-level control gating while preserving the existing persistence coverage in [settings-keychain.test.ts](/Users/cos_dev/CoS/projects/pokemon/runtime/workspaces/talkin-ai/apps/desktop/tests/settings-keychain.test.ts:65).

## Design Compliance Notes

- Status: `weak`
- `story-6.1:AC-5`
  - Source evidence passes: the screen groups controls into user-facing execution, response/display, and API-key-context sections instead of a raw developer preferences list.
  - State handling now also passes the review fix: loading and read-failure states no longer present preview defaults as current saved settings, and the read-error body stays on user-safe guidance.
  - Weakness: no live browser screenshot was captured because the Browser plugin had no available in-app target and the local renderer script still fails before `/settings` becomes reachable.

## Design Differentiation Notes

- Status: `weak`
- Intended archetype: `chat-first inbox`
- Visible choices made:
  - settings page uses a current-default hero card, distinct state cards, a grouped main control column, and a context rail rather than a generic single-column preferences list
  - copy stays task-oriented and Korean-first instead of reading like developer environment knobs
  - theme and advanced-preview effects are surfaced as workflow consequences, not raw booleans
- Layout silhouette difference from default/control:
  - the page reads like a desktop control center attached to the workspace shell, not a centered form or generic admin dashboard
- Default convergence risks avoided:
  - avoided a cold SaaS settings table
  - avoided exposing API-key placeholders as raw env-var instructions
- Remaining weak spots:
  - no live rendered screenshot evidence
  - the API key group is intentionally non-interactive until `story-6.2`

## Outcome Quality Notes

- Purpose clarity:
  - strong by source; the header and grouped controls explain that this screen sets default model, optimization, response, theme, and history behavior.
- Primary action:
  - strong; each preference is directly selectable from the same surface with immediate save feedback.
- State/copy handling:
  - loading, preview fallback, safe save error, retry, and save success are explicitly handled, and loading/read-failure no longer misrepresent preview values as current saved defaults.
- Visible quality choices:
  - shared renderer settings state keeps theme and advanced preview tied to visible app behavior instead of only persisting hidden values.
- Remaining weak spots:
  - live visual confirmation is still missing because the Browser plugin had no available in-app target and the local renderer launch path is still blocked.
  - package readiness remains blocked by the pre-existing `vite/client` typecheck issue.

## Structured Assessment

```json
{
  "success_criteria_status": "not_met",
  "story_completion_status": "incomplete",
  "mvp_acceptance_status": "not_met",
  "release_evidence_status": "partial",
  "package_readiness_status": "not_ready",
  "remaining_required_story_keys": [
    "story-6.1",
    "story-6.2",
    "story-6.3",
    "story-6.4"
  ],
  "next_story_candidates": [
    "story-6.1",
    "story-6.2",
    "story-6.3",
    "story-6.4"
  ],
  "risks": [
    "Live settings-screen verification is still GUIDED_VERIFICATION_REQUIRED because the Browser plugin exposed no in-app target and the local renderer launch path still fails before /settings becomes reachable.",
    "Package typecheck still fails on the pre-existing missing 'vite/client' type definition.",
    "API key entry and provider health remain intentionally deferred to story-6.2."
  ]
}
```
